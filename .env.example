# ===========================================
# Kushu TRIP 환경변수 설정 파일
# ===========================================
# 이 파일을 .env로 복사하고 실제 API 키들을 입력하세요
# cp .env.example .env

# Google Places API 키 (필수)
# https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_PLACES_API_KEY=YOUR_GOOGLE_PLACES_API_KEY_HERE

# Google Gemini AI API 키 (필수)
# https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Brave Search API 키 (선택사항)
# https://api.search.brave.com/app/keys
VITE_BRAVE_SEARCH_API_KEY=YOUR_BRAVE_SEARCH_API_KEY_HERE

# 백엔드 API URL (선택사항)
# 기본값: http://localhost:5000/api
VITE_API_BASE_URL=http://localhost:5000/api

# MongoDB Atlas 연결 문자열 (백엔드용)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority

# 개발/테스트 설정
VITE_USE_MOCK_API=false
VITE_USE_REAL_SCRAPING=true

# ===========================================
# API 키 발급 가이드
# ===========================================

# 1. Google Places API
#    - Google Cloud Console에서 프로젝트 생성
#    - Places API 활성화
#    - API 키 생성 후 Places API 사용 권한 설정

# 2. Google Gemini AI API
#    - Google AI Studio에서 API 키 생성
#    - 무료 할당량: 월 60회 요청

# 3. Brave Search API
#    - 트렌딩 데이터 수집용 (선택사항)
#    - 무료 할당량: 월 2,000회 요청

# 4. MongoDB Atlas
#    - MongoDB Atlas에서 클러스터 생성
#    - 백엔드 데이터 캐싱용 (선택사항)
